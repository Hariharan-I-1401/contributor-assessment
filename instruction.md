There is a text file located at `/app/input.txt` that contains several lines of text interspersed with empty lines.

Your task is to:
1. Read `/app/input.txt` and remove all empty or whitespace-only lines.
2. Count the total number of words remaining across all non-empty lines (words are separated by spaces).
3. Write only that total count integer into a new file located exactly at `/app/output.txt`.

For example, if the remaining text has exactly 15 words, `/app/output.txt` should contain just `15`. Do not add any extra text or labels to the output file.